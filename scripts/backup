#!/bin/bash
GIT="/usr/bin/git"

# backup dwm setup
DWM_DIR="$HOME/.dwm-setup"
cd $DWM_DIR || exit
if [[ -n "$($GIT status -b --porcelain)" ]]; then
  git add . >/dev/null 2>&1
  git commit -m "Automated update: $(date --rfc-3339 s)" >/dev/null 2>&1
  git push >/dev/null 2>&1
fi

# backup dotfiles
DOTFILES_DIR="$HOME/.dotfiles"
cd $DOTFILES_DIR || exit
if [[ -n "$($GIT status -b --porcelain)" ]]; then
  git add . >/dev/null 2>&1
  git commit -m "Automated update: $(date --rfc-3339 s)" >/dev/null 2>&1
  git push >/dev/null 2>&1
fi
